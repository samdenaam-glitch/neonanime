[build]
  publish = "."

[build.environment]
  # These will be injected at build time; set them in Netlify UI
  SUPABASE_URL = "https://your-project.supabase.co"
  SUPABASE_ANON_KEY = "your-anon-key"

# Redirects for SPA (if needed)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
